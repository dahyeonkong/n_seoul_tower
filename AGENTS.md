# [NseoulTower] 작업 규칙

## 0. 문서 목적

이 문서는 N SEOUL TOWER 공식 웹사이트 리뉴얼 프로젝트의 메인 페이지를 구현하거나 수정할 때 반드시 지켜야 하는 작업 기준입니다.

대상 화면은 첨부된 메인 페이지 디자인을 기준으로 하며, 주요 섹션은 다음과 같습니다.

- 글로벌 헤더
- 히어로
- Events
- Recommended Course
- N Pass
- N Gift Shop
- 커스텀 타워 조립 비주얼
- Custom Goods
- 푸터

기준 해상도는 다음과 같습니다.

- 모바일: 360px
- 태블릿: 834px
- 데스크톱: 1920px

데스크톱 1920px 디자인을 우선 시각 기준으로 사용하되, 모바일과 태블릿에서 동일한 정보 구조와 브랜드 경험이 유지되어야 합니다.

---

## 1. 작업 전 확인

작업을 시작하기 전에 아래 자료를 반드시 순서대로 확인합니다.

1. `PRD.md`
2. `DESIGN_ANALYSIS.md` 또는 디자인 분석 문서
3. 제공된 Figma 링크 또는 메인 페이지 이미지
4. `package.json`
5. lock 파일
6. 빌드 설정 파일
7. 기존 폴더 구조
8. 공통 HTML 구조
9. 공통 CSS와 디자인 토큰
10. 공통 JavaScript
11. 기존 이미지, 아이콘, 폰트 에셋

### 1.1 프로젝트 구조 확인

아래 항목을 먼저 확인한 뒤 변경 범위를 결정합니다.

- 메인 진입 파일
  - 예: `index.html`
- 메인 전용 스타일 파일
  - 예: `css/main.css`
- 공통 스타일 파일
  - 예: `css/common.css`
- 메인 전용 JavaScript
  - 예: `js/main.js`
- 공통 JavaScript
  - 예: `js/common.js`
- 에셋 폴더
  - 예: `assets/images`
  - 예: `assets/icons`
  - 예: `assets/fonts`
- 기존 라이브러리
  - GSAP
  - ScrollTrigger
  - Swiper
  - Lenis
  - 기타 프로젝트에 이미 포함된 라이브러리

실제 저장소에서 확인되지 않은 파일명과 경로를 임의로 만들지 않습니다.

### 1.2 확인된 사실과 추정 구분

작업 전 아래 형식으로 간단히 기록합니다.

```md
## 확인된 사실

- 메인 페이지는 HTML, CSS, JavaScript로 구성되어 있습니다.
- 공통 헤더 스타일은 `확인한 실제 경로`에 있습니다.
- 프로젝트에서 Swiper를 이미 사용하고 있습니다.

## 확인이 필요한 내용

- 모바일 메뉴의 최종 디자인
- 이벤트 카드의 실제 링크
- N Pass CTA의 목적지
```

확인하지 못한 내용을 확정된 요구사항처럼 구현하지 않습니다.

---

## 2. 변경 원칙

- 사용자가 요청한 화면과 기능만 수정합니다.
- 기존 코드, 폴더 구조, 디자인 토큰, 명명 규칙을 최대한 유지합니다.
- 관련 없는 리팩터링을 하지 않습니다.
- 관련 없는 파일명을 변경하지 않습니다.
- 요청받지 않은 기능을 재작성하지 않습니다.
- 기존 공통 컴포넌트와 유틸리티를 먼저 재사용합니다.
- 디자인에 이미 존재하는 에셋을 CSS 도형이나 신규 이미지로 대체하지 않습니다.
- 존재하지 않는 API, 경로, 이미지, 아이콘, 폰트를 임의로 만들지 않습니다.
- 요청 없이 패키지나 외부 라이브러리를 추가하지 않습니다.
- 기존 동작을 제거하거나 변경해야 할 경우 영향 범위를 먼저 확인합니다.
- 한 섹션 수정 때문에 다른 섹션의 스타일이 변경되지 않도록 선택자 범위를 제한합니다.
- 구현 편의를 위해 디자인을 단순화하지 않습니다.
- 축소 캡처만 보고 정확한 수치를 단정하지 않습니다.
- Figma 또는 실제 에셋이 없는 부분은 `TODO` 또는 확인 필요 항목으로 남깁니다.

---

## 3. 기술 스택

### 3.1 기본 기술

- HTML5
- CSS3
- Vanilla JavaScript

### 3.2 추가 금지 기술

별도 요청이 없는 한 다음 기술을 추가하지 않습니다.

- React
- Vue
- Svelte
- TypeScript
- Tailwind CSS
- Bootstrap
- Three.js
- 신규 번들러
- 신규 CSS 프레임워크

### 3.3 사용 가능한 라이브러리

아래 라이브러리는 프로젝트에 이미 설치되어 있거나 기존 코드에서 사용 중인 경우에만 사용합니다.

- GSAP
- ScrollTrigger
- Swiper
- Lenis

사용 전 확인 항목:

- `package.json` 의존성
- 기존 import 또는 script 태그
- 기존 초기화 코드
- 라이선스
- 동일 기능의 공통 코드 존재 여부

라이브러리가 저장소에 없으면 설치하지 않고 기본 CSS와 JavaScript로 구현합니다.

---

## 4. HTML

### 4.1 의미 구조

아래 시맨틱 요소를 역할에 맞게 사용합니다.

- `header`: 글로벌 헤더
- `nav`: 주요 메뉴
- `main`: 메인 콘텐츠
- `section`: 각 콘텐츠 영역
- `article`: 독립적인 이벤트 또는 상품 카드
- `footer`: 사이트 푸터

권장 구조:

```html
<body>
  <div class="site_wrapper">
    <header class="site_header">
      <!-- logo, navigation, utilities -->
    </header>

    <main class="main_content">
      <section class="hero_section">
        <!-- hero -->
      </section>

      <section class="events_section">
        <!-- events -->
      </section>

      <section class="recommended_course_section">
        <!-- recommended course -->
      </section>

      <section class="n_pass_section">
        <!-- n pass -->
      </section>

      <section class="gift_shop_section">
        <!-- gift shop -->
      </section>

      <section class="custom_tower_section">
        <!-- tower parts -->
      </section>

      <section class="custom_goods_section">
        <!-- custom goods -->
      </section>
    </main>

    <footer class="site_footer">
      <!-- footer -->
    </footer>
  </div>
</body>
```

### 4.2 요소 사용 원칙

- 동작을 실행하는 요소는 `button`을 사용합니다.
- 다른 페이지로 이동하는 요소는 `a`를 사용합니다.
- 카드 전체가 링크인 경우 하나의 명확한 링크 구조를 사용합니다.
- `div`에 클릭 이벤트만 연결하지 않습니다.
- 모든 입력은 `label`과 연결합니다.
- 아이콘 대신 이모지를 사용하지 않습니다.
- 장식 요소는 접근성 트리에서 제외합니다.
- 제목 구조는 `h1`부터 순서대로 사용합니다.
- 메인 페이지의 `h1`은 `N SEOUL TOWER` 하나만 사용합니다.
- 섹션 제목은 `h2`, 카드 제목은 `h3`를 사용합니다.
- 의미 없는 `<br>` 반복으로 레이아웃을 맞추지 않습니다.
- 빈 링크와 `href="#"`를 임시 이동 수단으로 사용하지 않습니다.
- 링크가 확정되지 않았으면 비활성 상태 또는 `TODO`로 남깁니다.

### 4.3 이미지

- 의미 있는 이미지에는 구체적인 `alt`를 작성합니다.
- 장식 이미지는 `alt=""`를 사용합니다.
- 이미지에 `width`, `height` 또는 `aspect-ratio`를 지정합니다.
- 히어로 핵심 이미지는 lazy loading을 사용하지 않습니다.
- 첫 화면 아래 이미지는 `loading="lazy"`를 적용할 수 있습니다.
- 배경 이미지에 필수 정보를 포함하지 않습니다.
- 상품 누끼 이미지는 `object-fit: contain`을 우선 사용합니다.
- 이벤트와 코스 이미지는 `object-fit: cover`를 우선 사용합니다.

---

## 5. CSS

### 5.1 기존 스타일 우선

- 기존 CSS 변수와 디자인 토큰을 먼저 사용합니다.
- 동일한 색상, 간격, 라운드, 그림자를 새 값으로 중복 선언하지 않습니다.
- 공통 버튼과 카드 스타일이 있으면 확장하여 사용합니다.
- 페이지 전용 스타일은 메인 페이지 범위 안에서 작성합니다.
- 전역 태그 선택자 변경을 최소화합니다.

### 5.2 명명 규칙

- CSS class와 HTML id는 `snake_case`를 사용합니다.
- 상태 class는 다음 형식을 사용합니다.
  - `is_active`
  - `is_open`
  - `is_selected`
  - `is_disabled`
  - `is_loading`
- 오류 class는 `has_error`를 사용합니다.
- 역할이 분명한 `data-*` 속성을 JavaScript 선택자로 사용합니다.

예시:

```html
<section class="events_section" data-events-section>
  <article class="event_card">
    <!-- content -->
  </article>
</section>
```

### 5.3 작성 원칙

- 불필요한 `!important`를 사용하지 않습니다.
- 선택자 깊이는 가능하면 3단계 이하로 유지합니다.
- 고정 높이는 디자인상 필요한 경우에만 사용합니다.
- 텍스트 영역에 과도한 고정 너비를 사용하지 않습니다.
- 레이아웃은 Flexbox 또는 Grid를 우선 사용합니다.
- 절대 위치는 장식 오브젝트나 디자인상 필요한 겹침에만 사용합니다.
- 절대 위치 요소는 반드시 기준 컨테이너를 명확히 지정합니다.
- 장식 요소로 페이지 전체 가로 스크롤이 생기지 않게 합니다.
- `100vw`로 인한 스크롤바 오버플로를 주의합니다.
- 이미지 비율을 강제로 왜곡하지 않습니다.
- 디자인에 없는 그림자, 테두리, 그라데이션을 임의로 추가하지 않습니다.
- 모바일에서 hover 효과에 의존하지 않습니다.

### 5.4 디자인 토큰

실제 프로젝트 토큰이 존재하면 아래 임시값 대신 기존 토큰을 사용합니다.

```css
:root {
  --color_bg: #f6f3e6;
  --color_surface: #ffffff;
  --color_text: #313232;
  --color_title: #223147;
  --color_green_dark: #265147;
  --color_green: #969a6c;
  --color_green_light: #b3c891;
  --color_yellow: #fdd773;
  --color_accent_warm: #cdaa70;
  --color_line: rgba(49, 50, 50, 0.16);

  --space_1: 4px;
  --space_2: 8px;
  --space_3: 12px;
  --space_4: 16px;
  --space_5: 24px;
  --space_6: 32px;
  --space_7: 48px;
  --space_8: 64px;
  --space_9: 96px;
  --space_10: 128px;

  --radius_small: 8px;
  --radius_medium: 16px;
  --radius_large: 24px;
  --radius_round: 999px;

  --shadow_card: 0 8px 24px rgba(49, 50, 50, 0.08);

  --content_max_width: 1680px;
}
```

위 값은 첨부 이미지 기준의 근사값이며, Figma Variables 또는 기존 CSS 값을 확인한 뒤 교체합니다.

---

## 6. 반응형

### 6.1 기준 해상도

- 모바일: 360px
- 태블릿: 834px
- 데스크톱: 1920px

### 6.2 작성 방식

모바일부터 작성하고 다음 기준으로 확장합니다.

```css
/* Mobile: 360px 기준 기본 스타일 */

@media (min-width: 834px) {
  /* Tablet */
}

@media (min-width: 1280px) {
  /* Desktop layout */
}

@media (min-width: 1920px) {
  /* Design reference width */
}
```

1920px만 맞추고 중간 해상도를 방치하지 않습니다. 최소한 아래 크기를 직접 확인합니다.

- 360px
- 834px
- 1280px
- 1920px

### 6.3 모바일 360px

- 모든 주요 콘텐츠는 1열 구조를 기본으로 합니다.
- 좌우 여백은 약 20px을 기준으로 하되 기존 토큰을 우선 사용합니다.
- 헤더는 로고, 메뉴 버튼, 언어 버튼 중심으로 단순화합니다.
- 히어로 제목은 2~3줄로 재배치합니다.
- 타워 이미지가 제목을 가리지 않도록 크기와 z-index를 조정합니다.
- 이벤트 카드는 세로 목록 또는 가로 스크롤로 변경합니다.
- 이벤트 점선 경로는 세로형 또는 단순한 곡선으로 축소합니다.
- 추천 코스 정보 패널은 이미지 아래 또는 이미지 내부 하단으로 이동합니다.
- N Pass는 세로형 티켓 구조로 변경할 수 있습니다.
- Gift Shop과 Custom Goods는 가로 스크롤을 사용할 수 있습니다.
- 커스텀 타워 파츠는 화면 중앙에 배치합니다.
- 푸터 컬럼은 세로로 쌓습니다.
- 모든 버튼과 링크는 최소 44 × 44px의 터치 영역을 확보합니다.
- 페이지 전체 가로 스크롤이 없어야 합니다.

### 6.4 태블릿 834px

- 좌우 여백은 약 40px을 기준으로 합니다.
- 1열과 2열 구조를 혼합합니다.
- 헤더 메뉴가 모두 들어가지 않으면 모바일 메뉴 형태를 유지합니다.
- 히어로 제목과 타워 겹침은 유지하되 데스크톱보다 축소합니다.
- 이벤트 카드는 2열 또는 가로 스크롤로 구성합니다.
- 추천 코스는 가로형 카드를 유지할 수 있습니다.
- N Pass는 가로 티켓 구조를 유지하되 내부 여백을 조정합니다.
- Gift Shop은 3~4개 카드가 일부 보이는 가로 목록으로 구성합니다.
- 커스텀 타워 파츠의 좌우 이동 폭을 줄입니다.
- Custom Goods는 2열 또는 가로 목록으로 구성합니다.
- 푸터는 2~3열로 재배치합니다.
- 페이지 전체 가로 스크롤이 없어야 합니다.

### 6.5 데스크톱 1920px

- 첨부 디자인의 비정형 배치와 넓은 여백을 유지합니다.
- 최대 콘텐츠 폭은 약 1600~1680px 범위에서 원본과 비교해 결정합니다.
- 좌우 여백은 약 80~160px 범위에서 원본과 맞춥니다.
- 히어로 제목은 화면 폭을 크게 사용합니다.
- 타워 이미지는 제목 사이에 세로로 배치합니다.
- Events의 곡선 경로, 곤돌라, 카드 위치를 넓게 분산합니다.
- Recommended Course는 큰 가로형 비주얼 카드로 유지합니다.
- N Pass는 좌우로 긴 티켓 비율을 유지합니다.
- Gift Shop 상품 카드는 한 줄에 여러 개 노출합니다.
- 커스텀 타워 파츠는 긴 세로 스크롤과 비정형 위치를 유지합니다.
- Custom Goods는 설명, 대표 이미지, 카드 목록을 넓은 레이아웃으로 구성합니다.
- 푸터는 다단 컬럼으로 구성합니다.

---

## 7. JavaScript

### 7.1 명명 규칙

- 변수와 함수는 `camelCase`
- 불리언은 다음 접두어 사용
  - `is`
  - `has`
  - `can`
  - `should`
- 이벤트 함수는 `handleXxx`
- 렌더링 함수는 `renderXxx`
- 초기화 함수는 `initXxx`
- 전역 고정 상수만 `UPPER_SNAKE_CASE`

예시:

```js
const languageButton = document.querySelector("[data-language-button]");
const languageMenu = document.querySelector("[data-language-menu]");

let isLanguageMenuOpen = false;

function handleLanguageMenuToggle() {
  isLanguageMenuOpen = !isLanguageMenuOpen;
  renderLanguageMenuState();
}

function renderLanguageMenuState() {
  languageButton.setAttribute("aria-expanded", String(isLanguageMenuOpen));
  languageMenu.classList.toggle("is_open", isLanguageMenuOpen);
}
```

### 7.2 작성 원칙

- 중복 로직은 목적이 분명한 함수로 분리합니다.
- 단순한 한 줄 코드를 과도하게 함수화하지 않습니다.
- 전역 변수 사용을 최소화합니다.
- DOM이 존재하는지 확인한 뒤 이벤트를 연결합니다.
- 사용자 입력값을 사용 전에 검증합니다.
- `localStorage` 값은 파싱과 타입 확인 후 사용합니다.
- 데이터가 없을 때 예외가 발생하지 않도록 처리합니다.
- 임시 `console.log`는 완료 전에 제거합니다.
- 오류를 숨기기 위해 빈 `catch`를 사용하지 않습니다.
- 스크롤 이벤트에서 직접 반복 계산하지 않습니다.
- 뷰포트 진입 감지는 Intersection Observer를 우선 사용합니다.
- 애니메이션 프레임이 필요한 경우 `requestAnimationFrame`을 사용합니다.
- 링크가 없는 카드에 클릭 이벤트를 강제로 연결하지 않습니다.

### 7.3 로컬 저장소

언어 선택처럼 저장이 필요한 값만 사용합니다.

```js
const LANGUAGE_STORAGE_KEY = "n_seoul_tower_language";
```

저장하지 않는 정보:

- 이메일
- 개인정보
- 결제정보
- 방문 기록
- 사용자의 민감한 데이터

---

## 8. 디자인 구현

### 8.1 공통 원칙

- Figma 또는 제공된 디자인 원본을 시각 기준으로 사용합니다.
- 화면 구조, 간격, 정렬, 색상, 폰트, 상태를 임의로 재해석하지 않습니다.
- 디자인에 없는 섹션을 추가하지 않습니다.
- 디자인에 없는 장식 모션을 추가하지 않습니다.
- 실제 에셋을 우선 사용합니다.
- placeholder 이미지 URL을 만들지 않습니다.
- 임시 이미지를 최종 에셋처럼 사용하지 않습니다.
- 배경색과 카드색의 미세한 차이를 무시하지 않습니다.
- 긴 페이지의 빈 공간을 오류로 보고 임의로 줄이지 않습니다.
- 비정형 레이아웃을 일반적인 동일 폭 그리드로 단순화하지 않습니다.

### 8.2 히어로

- 대형 `N SEOUL TOWER` 타이포그래피가 가장 먼저 인식되어야 합니다.
- 타워 이미지는 원본 비율을 유지합니다.
- 제목과 타워의 겹침 위치는 디자인 원본과 비교합니다.
- 날씨와 운영 정보는 확인된 위치에 배치합니다.
- 제목 텍스처가 실제 에셋이면 CSS 효과로 대체하지 않습니다.
- 작은 텍스트가 축소되어 읽기 어려워지지 않게 합니다.

### 8.3 Events

- 점선 곡선은 SVG를 우선 사용합니다.
- 곤돌라와 카드의 위치 차이를 유지합니다.
- 장식 오브젝트는 `pointer-events: none`을 적용합니다.
- 이벤트 카드 내부 구조를 반복 가능한 형태로 작성합니다.
- 실제 슬라이더가 확인되지 않으면 Swiper를 사용하지 않습니다.

### 8.4 Recommended Course

- 배경 이미지와 캐릭터, 타워, 정보 패널이 하나의 장면처럼 보여야 합니다.
- 정보 패널은 이미지 내용과 충돌하지 않게 배치합니다.
- 이미지의 중요 영역이 반응형에서 잘리지 않도록 `object-position`을 조정합니다.
- CTA 위치와 크기는 디자인 원본을 따릅니다.

### 8.5 N Pass

- 좌우로 긴 티켓 형태를 유지합니다.
- 티켓 절취부는 CSS 가상 요소 또는 실제 에셋을 사용합니다.
- 좌측 밝은 영역과 우측 딥그린 영역의 비율을 유지합니다.
- 모바일에서는 콘텐츠 가독성을 위해 상하 구조로 전환할 수 있습니다.
- 실제 결제 기능은 구현하지 않습니다.

### 8.6 N Gift Shop

- 상품 카드는 배경 제거 이미지 중심으로 구성합니다.
- 카드별 미세한 회전과 높이 차이가 디자인에 있다면 유지합니다.
- 모바일과 태블릿에서는 가로 스크롤을 허용합니다.
- 카드 이미지가 잘리지 않도록 `contain`을 사용합니다.
- 가격이나 재고가 확인되지 않았으면 임의로 추가하지 않습니다.

### 8.7 커스텀 타워 조립 비주얼

- 제공된 개별 파츠 에셋을 우선 사용합니다.
- 파츠 순서를 임의로 변경하지 않습니다.
- 긴 세로 간격을 디자인의 일부로 유지합니다.
- 스크롤 모션이 확인되지 않았다면 정적 배치부터 정확히 구현합니다.
- 모션을 추가할 경우 파츠가 순차적으로 등장하는 정도로 제한합니다.
- `prefers-reduced-motion`에서는 정적인 배치를 제공합니다.

### 8.8 Custom Goods

- 소개 텍스트, 대표 타워 이미지, 상품 카드의 계층을 유지합니다.
- 카드 목록은 반복 가능한 데이터 구조로 작성합니다.
- 다음 버튼이 있다면 실제 목록 이동과 연결합니다.
- 상품명과 설명 길이에 따라 카드 높이가 무너지지 않게 합니다.
- 디자인에 없는 가격, 배지, 찜 버튼을 추가하지 않습니다.

### 8.9 푸터

- 다크 배경과 타워 실루엣을 유지합니다.
- 사이트맵, 운영 정보, 저작권의 계층을 구분합니다.
- 입력 UI가 실제 기능인지 확인합니다.
- 실제 API가 없으면 목업 제출 상태만 구현하거나 비활성 처리합니다.
- 모바일에서는 링크 그룹을 세로로 배치합니다.

---

## 9. Figma MCP

Figma MCP를 사용할 수 있는 경우 다음 순서로 확인합니다.

1. 파일 또는 프레임 구조 확인
2. 대상 화면 스크린샷 확인
3. 레이어와 Auto Layout 확인
4. Variables와 Styles 확인
5. Text Style 확인
6. 이미지와 아이콘 에셋 확인
7. Prototype 인터랙션 확인
8. 필요한 에셋 다운로드
9. 현재 기술 스택에 맞게 구현
10. 브라우저 결과와 Figma 비교

### 9.1 Figma에서 확인할 항목

- 프레임 크기
- Grid
- Constraints
- Auto Layout
- Gap
- Padding
- 색상 변수
- 폰트명
- font-weight
- line-height
- letter-spacing
- border-radius
- shadow
- 이미지 fill 방식
- hover 상태
- disabled 상태
- 모바일 메뉴
- 슬라이더 여부
- 스크롤 모션
- 고정 요소

### 9.2 사용 원칙

- Figma MCP가 생성한 코드를 그대로 붙이지 않습니다.
- React 또는 Tailwind 코드가 생성되어도 현재 HTML, CSS, JavaScript 구조로 해석합니다.
- 인라인 스타일을 그대로 복사하지 않습니다.
- 실제 에셋은 제공된 다운로드 기능을 사용합니다.
- 에셋 이름을 프로젝트 규칙에 맞게 정리하되 기존 참조를 깨지 않습니다.
- 구현 후 실제 브라우저 화면을 동일 해상도 스크린샷과 비교합니다.

---

## 10. 접근성과 상태

### 10.1 키보드 접근성

- 모든 메뉴, 버튼, 링크를 Tab으로 이동할 수 있어야 합니다.
- 시각적 순서와 Tab 순서가 일치해야 합니다.
- `focus-visible` 상태를 명확하게 표시합니다.
- 모바일 메뉴는 ESC로 닫을 수 있어야 합니다.
- 메뉴를 닫은 뒤 포커스가 메뉴 버튼으로 돌아가야 합니다.
- 숨겨진 메뉴 항목은 포커스 대상에서 제외합니다.

### 10.2 상태 구현

필요한 상태:

- 기본
- hover
- focus-visible
- pressed
- loading
- empty
- error
- disabled

### 10.3 로딩 상태

- 이미지 비율을 먼저 확보해 레이아웃 이동을 방지합니다.
- 전체 페이지 스피너를 임의로 추가하지 않습니다.
- 첫 화면 아래 이미지만 lazy loading을 사용합니다.
- 데이터 로딩이 실제로 존재하는 경우에만 스켈레톤을 구현합니다.

### 10.4 빈 상태

- 이벤트 없음:
  - `현재 진행 중인 이벤트가 없습니다.`
- 상품 없음:
  - `상품 준비 중입니다.`
- 추천 코스 없음:
  - 운영 정책 확인 후 섹션 숨김 또는 기본 콘텐츠 노출

빈 상태 문구는 실제 운영 문구가 있으면 해당 문구를 사용합니다.

### 10.5 오류 상태

- 이미지 실패 시 대체 이미지 또는 배경을 표시합니다.
- 데이터 오류 시 원인과 다음 행동을 안내합니다.
- 링크가 없는 CTA는 비활성화합니다.
- 오류 상태를 색상만으로 표현하지 않습니다.

### 10.6 비활성 상태

- `button`은 실제 `disabled` 속성을 사용합니다.
- 링크형 요소는 `aria-disabled="true"`와 클릭 차단을 함께 처리합니다.
- 비활성 상태에서는 hover 효과를 제거합니다.

### 10.7 모션 접근성

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

- 자동 재생되는 강한 움직임을 사용하지 않습니다.
- 곤돌라 부유 효과와 타워 파츠 조립 효과는 reduced motion에서 제거합니다.
- 모션이 없어도 모든 정보를 이해할 수 있어야 합니다.

---

## 11. 성능

- 이미지는 WebP 또는 AVIF를 우선 검토합니다.
- 원본 품질이 필요한 투명 누끼는 WebP 또는 PNG를 사용합니다.
- 해상도별 적절한 이미지 소스를 제공합니다.
- `srcset`과 `sizes`를 사용합니다.
- 모든 이미지에 크기 정보를 지정합니다.
- 첫 화면 핵심 에셋은 preload 또는 `fetchpriority="high"`를 검토합니다.
- 사용하지 않는 폰트 굵기를 로드하지 않습니다.
- 스크롤 이벤트를 과도하게 등록하지 않습니다.
- 레이아웃 스래싱을 유발하는 반복 DOM 측정을 피합니다.
- 보이지 않는 장식 에셋을 무조건 모두 선로드하지 않습니다.
- 사용하지 않는 라이브러리와 CSS를 제거합니다.
- 404 에셋 요청이 없어야 합니다.

---

## 12. 검증

### 12.1 실행 명령 확인

실제 명령은 `package.json`과 저장소 설정을 확인한 후 기록합니다.

확인 전에는 아래 명령을 실제 프로젝트 명령으로 단정하지 않습니다.

```bash
# 예시일 뿐이며 package.json 확인 후 교체
npm run lint
npm run test
npm run build
npm run dev
```

정적 HTML 프로젝트라면 별도 명령이 없을 수 있습니다. 이 경우 브라우저 직접 실행과 정적 서버 검증을 기록합니다.

예시:

```bash
# Python이 설치된 로컬 환경에서만 사용하는 예시
python -m http.server 8000
```

### 12.2 필수 검증 해상도

- 360 × 800
- 834 × 1112
- 1280 × 800
- 1920 × 1080

### 12.3 디자인 검증

- 디자인 원본과 브라우저 화면을 나란히 비교합니다.
- 히어로 타이포그래피 크기와 줄바꿈을 확인합니다.
- 타워 이미지의 위치와 비율을 확인합니다.
- Events 곡선과 곤돌라 위치를 확인합니다.
- Recommended Course 카드의 장면 구성을 확인합니다.
- N Pass 티켓 비율과 절취부를 확인합니다.
- Gift Shop 카드 회전과 간격을 확인합니다.
- 커스텀 타워 파츠의 순서와 간격을 확인합니다.
- Custom Goods 카드 배치를 확인합니다.
- 푸터 실루엣과 정보 정렬을 확인합니다.

### 12.4 기능 검증

- 모든 링크 클릭
- 모든 버튼 클릭
- 메뉴 열기와 닫기
- ESC로 메뉴 닫기
- 외부 클릭으로 메뉴 닫기
- 가로 스크롤
- 슬라이드 버튼
- 빈 데이터
- 링크 없는 데이터
- 이미지 로드 실패
- 비활성 상태

### 12.5 접근성 검증

- 키보드 Tab 순서
- `focus-visible`
- 이미지 alt
- 제목 계층
- 색상 대비
- 200% 확대
- reduced motion
- 스크린리더용 이름
- 모바일 메뉴 포커스 관리

### 12.6 코드 검증

- 콘솔 오류 없음
- 임시 `console.log` 없음
- 404 에셋 없음
- 사용하지 않는 코드 없음
- 잘못된 HTML 중첩 없음
- 중복 id 없음
- 불필요한 `!important` 없음
- 페이지 전체 가로 스크롤 없음

실행하지 않은 검증은 통과했다고 작성하지 않습니다.

---

## 13. 결과 보고

작업 완료 후 아래 항목을 구분하여 보고합니다.

```md
## 변경 파일

- index.html
- css/main.css
- js/main.js
- assets/...

## 구현 내용

- 히어로 레이아웃 구현
- Events 카드와 곡선 장식 구현
- Recommended Course 구현
- N Pass 티켓 구현
- N Gift Shop 가로 목록 구현
- 커스텀 타워 파츠 배치 구현
- Custom Goods 구현
- 푸터 구현
- 360px, 834px, 1920px 반응형 구현
- 접근성 상태 구현

## 주요 판단

- 이벤트 영역은 디자인에 실제 슬라이더가 확인되지 않아 정적 카드로 구현
- 모바일 N Pass는 가독성을 위해 세로형으로 전환
- 타워 조립 모션은 원본 확인 전 정적 배치로 구현

## 검증 결과

- lint: 실행 결과 또는 미실행 사유
- test: 실행 결과 또는 미실행 사유
- build: 실행 결과 또는 미실행 사유
- 360px: 통과 / 이슈
- 834px: 통과 / 이슈
- 1280px: 통과 / 이슈
- 1920px: 통과 / 이슈
- 키보드 접근성: 통과 / 이슈
- 콘솔 오류: 없음 / 있음

## 확인하지 못한 부분

- 실제 API
- 최종 링크
- 최종 다국어 문구
- 실제 상품 데이터
- Figma 프로토타입 모션
```

---

## 14. 금지 사항

- 디자인에 없는 섹션 추가
- 디자인에 없는 팝업 추가
- 디자인에 없는 무한 스크롤 추가
- 요청 없는 자동 재생 캐러셀
- 요청 없는 패럴랙스
- 요청 없는 Lenis 적용
- 요청 없는 GSAP 설치
- 실제 에셋 대신 외부 placeholder 사용
- 이모지를 아이콘으로 사용
- 임의의 가짜 상품명, 가격, 이벤트 기간 추가
- 임의의 API 응답 생성
- 링크가 없는 상태에서 `href="#"` 사용
- 접근성 없는 `div` 클릭 구현
- 데스크톱만 맞추고 모바일 방치
- 콘텐츠를 맞추기 위한 과도한 절대 위치
- 관련 없는 코드 정리와 리팩터링
- 검증하지 않은 결과를 통과로 보고
