# [Kbrand_NseoulTower] Main Page PRD

## 0. 프로젝트 배경

N SEOUL TOWER 공식 웹사이트 리뉴얼 프로젝트의 메인 페이지를 구현합니다.

메인 페이지는 단순한 시설 안내가 아니라, 방문 전 사용자가 N SEOUL TOWER의 분위기와 주요 경험을 빠르게 이해하고 다음 행동으로 이동할 수 있도록 설계합니다. 제공된 디자인 시안을 기준으로 이벤트, 추천 코스, N Pass, 기프트숍, 커스텀 굿즈를 하나의 긴 스크롤 경험으로 연결합니다.

이번 문서는 **데스크톱 1920px 화면을 우선 기준**으로 작성하며, 태블릿 834px과 모바일 360px에서도 콘텐츠 구조와 핵심 경험이 유지되도록 반응형으로 구현합니다.

---

## 1. 제품 개요

서울을 방문하거나 N SEOUL TOWER 방문을 계획하는 국내외 사용자가 이벤트, 추천 코스, 입장 상품, 굿즈 정보를 한 페이지에서 탐색하고 상세 페이지로 이동할 수 있는 브랜드형 메인 페이지를 제작합니다.

---

## 2. 문제 정의

- 기존 사이트는 방문자가 원하는 정보를 메뉴 단위로 찾아야 하므로 전체 경험을 한눈에 이해하기 어렵습니다.
- 이벤트, 코스, 이용권, 굿즈가 서로 분리되어 있어 다음 탐색 행동으로 자연스럽게 이어지지 않습니다.
- 모바일과 태블릿에서 이미지, 텍스트, 카드의 우선순위가 불분명하면 긴 페이지의 피로도가 높아질 수 있습니다.
- N SEOUL TOWER만의 상징성과 감성적인 브랜드 경험이 일반적인 관광 안내 페이지처럼 보일 수 있습니다.
- 제공된 디자인 시안의 비정형 레이아웃과 스크롤 연출을 구현할 때, 임의의 이미지·텍스트·인터랙션을 추가하면 원본 디자인과 달라질 수 있습니다.

---

## 3. 목표 사용자

### 3.1 주요 사용자

- 서울 여행 중 N SEOUL TOWER 방문을 계획하는 국내외 관광객
- 데이트, 가족 나들이, 사진 촬영, 식사 등 목적에 맞는 방문 코스를 찾는 사용자
- 진행 중인 이벤트와 시즌 콘텐츠를 확인하려는 사용자
- N Pass 또는 관련 이용권 정보를 확인하려는 사용자
- N SEOUL TOWER 굿즈와 커스텀 상품에 관심 있는 사용자

### 3.2 사용 환경

- 데스크톱 1920px: 여행 계획 단계에서 전체 콘텐츠를 넓은 화면으로 탐색
- 태블릿 834px: 이동 전 또는 현장에서 콘텐츠를 확인
- 모바일 360px: 현장에서 이벤트, 코스, 이용권, 위치 정보를 빠르게 탐색

---

## 4. 제품 목표

- 사용자는 첫 화면에서 N SEOUL TOWER의 브랜드와 분위기를 즉시 인지할 수 있어야 합니다.
- 사용자는 진행 중인 이벤트를 확인하고 이벤트 상세 페이지로 이동할 수 있어야 합니다.
- 사용자는 추천 코스의 대표 콘텐츠를 확인하고 코스 상세 페이지로 이동할 수 있어야 합니다.
- 사용자는 N Pass의 핵심 혜택을 이해하고 관련 상세 또는 구매 페이지로 이동할 수 있어야 합니다.
- 사용자는 기프트숍과 커스텀 굿즈를 탐색하고 각 상품 상세 또는 안내 페이지로 이동할 수 있어야 합니다.
- 긴 스크롤 페이지에서도 섹션 간 흐름이 끊기지 않고 하나의 브랜드 스토리처럼 느껴져야 합니다.
- 제공된 디자인 시안의 구도, 여백, 비정형 배치, 톤앤매너를 최대한 동일하게 구현해야 합니다.
- 360px, 834px, 1920px에서 잘림, 겹침, 가로 스크롤 없이 동작해야 합니다.

---

## 5. 제외 범위

- 로그인, 회원가입, 마이페이지
- 실제 결제 및 주문 처리
- 장바구니, 배송지 입력, 주문 내역
- 실제 예약 API 또는 티켓 발권 시스템 연동
- 실제 날씨 API 연동
- 관리자 페이지 및 콘텐츠 등록 기능
- 다국어 번역 시스템의 실제 서버 연동
- 디자인 시안에 없는 신규 섹션 또는 신규 기능
- 디자인 원본에 없는 이미지, 아이콘, 장식 요소의 임의 생성
- 별도 요청이 없는 3D WebGL, Canvas, Three.js 구현

---

## 6. 디자인 기준

### 6.1 기준 자료

- 제공된 메인 페이지 디자인 이미지 또는 Figma 원본을 최우선 시각 기준으로 사용합니다.
- 이미지에서 정확히 식별되지 않는 문구, 수치, 링크는 임의 확정하지 않고 목업 데이터로 분리합니다.
- 구현 전 기존 `assets` 폴더에서 로고, 타워, 곤돌라, 캐릭터, 티켓, 굿즈, 배경 이미지를 먼저 확인합니다.
- 디자인에 이미 존재하는 에셋을 CSS 도형이나 새 이미지로 대체하지 않습니다.

### 6.2 시각 방향

- 전체 배경은 따뜻한 아이보리 또는 크림 계열을 사용합니다.
- 제목은 진한 네이비 또는 블랙 계열의 강한 대비를 사용합니다.
- 보조 색상은 올리브 그린, 딥 그린, 옐로 계열을 디자인 시안에서 추출해 사용합니다.
- 카드와 티켓은 과도한 그림자보다 얕은 그림자, 라운드, 절제된 테두리를 사용합니다.
- 섹션은 일반적인 동일 폭 카드 그리드가 아니라, 디자인 시안처럼 크기와 위치에 변화를 둔 편집형 레이아웃을 유지합니다.
- 브랜드 캐릭터와 타워 오브젝트는 장식이 아니라 섹션 전환과 시선 유도를 담당합니다.

### 6.3 디자인 토큰

정확한 값은 Figma 또는 원본 CSS를 확인한 뒤 아래 CSS 변수에 반영합니다.

```css
:root {
  --color_bg: #f5f0df;
  --color_text: #0b2035;
  --color_text_muted: #6d6a5f;
  --color_green: #526d3a;
  --color_green_dark: #1f4e3b;
  --color_yellow: #f5bd33;
  --color_surface: #ffffff;
  --color_footer: #2d2d2d;

  --radius_small: 8px;
  --radius_medium: 16px;
  --radius_large: 24px;

  --shadow_card: 0 8px 24px rgba(0, 0, 0, 0.08);

  --content_max_width: 1680px;
  --section_space_desktop: 180px;
  --section_space_tablet: 120px;
  --section_space_mobile: 88px;
}
```

- 위 값은 임시 기준이며, 디자인 원본 확인 후 교체합니다.
- 개별 요소에서 임의의 색상값과 간격을 반복 선언하지 않습니다.

---

## 7. 핵심 기능

### 7.1 글로벌 헤더

- 로고 클릭 시 메인 페이지 최상단으로 이동합니다.
- 메뉴 항목 클릭 시 해당 상세 페이지로 이동합니다.
- 언어 선택 버튼 클릭 시 언어 메뉴가 열리고 닫힙니다.
- 날씨 또는 운영 정보는 디자인 시안에 표시된 범위에서만 노출합니다.
- 스크롤 시 헤더 고정 여부는 Figma 프로토타입 또는 기존 구현을 우선 확인합니다.

### 7.2 히어로 영역

- 대형 `N SEOUL TOWER` 타이포그래피와 타워 이미지를 결합해 첫 화면의 핵심 비주얼을 구성합니다.
- 데스크톱에서는 텍스트 일부와 타워 오브젝트가 겹치는 편집형 구도를 유지합니다.
- 날씨, 날짜, 운영 정보 등 보조 정보는 디자인 시안의 위치와 형식을 따릅니다.
- 타워 동영상 뒤로 보이는 텍스트는 
- 첫 화면 높이는 데스크톱에서 최소 한 뷰포트에 가까운 몰입감을 제공하되, 다음 섹션의 존재를 일부 인지할 수 있도록 조정합니다.

### 7.3 이벤트 영역

- 이벤트 카드 목록을 표시합니다.
- 각 카드에는 이미지, 카테고리 또는 기간, 제목, 짧은 설명, 이동 버튼을 포함할 수 있습니다.
- 카드 클릭 시 이벤트 상세 페이지로 이동합니다.
- 디자인 시안의 곡선형 점선 경로와 곤돌라 오브젝트를 유지합니다.
- 장식 오브젝트는 콘텐츠 클릭을 방해하지 않으며 `pointer-events: none`을 적용합니다.
- 이벤트가 3개 이상일 경우 실제 슬라이더가 디자인에 존재할 때만 Swiper 사용을 허용합니다.

### 7.4 추천 코스 영역

- 대표 추천 코스 1개를 큰 비주얼 카드로 노출합니다.
- 카드에는 코스 유형, 제목, 대표 이미지, 짧은 소개, 상세 이동 버튼을 포함합니다.
- 배경 이미지와 캐릭터, 타워, 자연 요소가 하나의 장면처럼 보이도록 구현합니다.
- 카드 클릭 또는 CTA 클릭 시 추천 코스 상세 페이지로 이동합니다.
- 추천 코스 데이터가 변경되어도 동일한 컴포넌트 구조를 재사용할 수 있어야 합니다.

### 7.5 N Pass 영역
- 코스 영역에서 티켓이 떨어지는 모습으로 시작한다.
- 티켓 형태의 시각 요소로 N Pass의 프리미엄 경험을 소개합니다.
- 제목, 짧은 설명, CTA 버튼을 제공합니다.
- CTA 클릭 시 N Pass 상세 또는 구매 안내 페이지로 이동합니다.
- 데스크톱에서는 좌우로 긴 티켓 구도를 유지합니다.
- 양 끝의 티켓 절취 형태는 CSS 마스크, 가상 요소 또는 제공 이미지 중 디자인과 가장 가까운 방식을 사용합니다.
- 실제 구매 기능은 제외하며 링크 이동만 구현합니다.

### 7.6 N Gift Shop 영역

- 기프트숍 대표 상품을 가로 방향 카드 목록으로 노출합니다.
- 각 상품 카드에는 이미지와 상품명 또는 카테고리를 표시합니다.
- 카드 클릭 시 상품 상세 또는 기프트숍 페이지로 이동합니다.
- 데스크톱에서는 여러 상품이 한 화면에 보이는 가로 배열을 사용합니다.
- 데스크톱에서 마우스커서의 모양이 버튼모양으로 변경됩니다.
- 데스크톱에서 가로 배열은 왼쪽으로 계속 스크롤링 됩니다.
- 모바일에서는 가로 스크롤 또는 스냅 리스트로 전환합니다.
- 실제 상품 가격과 재고는 목업 데이터로 관리합니다.

### 7.7 타워 조립형 커스텀 굿즈 비주얼

- 여러 파츠가 분리된 상태에서 완성된 타워 오브젝트로 이어지는 세로형 비주얼을 구현합니다.
- 데스크톱에서 오브젝트는 디자인 시안의 비정형 축을 따라 배치합니다.
- 스크롤 진입 시 각 파츠가 순서대로 나타나는 연출을 적용합니다.
- 모션은 콘텐츠 이해를 방해하지 않아야 하며, `prefers-reduced-motion`에서는 정적인 완성 상태 또는 단순 페이드로 대체합니다.
- 해당 영역은 장식 중심이므로 별도 클릭 기능을 강제하지 않습니다.

### 7.8 Custom Goods 영역

- 커스텀 굿즈 소개 문구, 대표 완성 이미지, 상품 카드 목록을 제공합니다.
- 상품 카드에는 이미지, 상품명, 짧은 설명을 포함합니다.
- 카드 클릭 시 상세 안내 페이지로 이동합니다.
- 우측 또는 하단에 다음 콘텐츠로 이동하는 화살표 버튼이 있는 경우 실제 목록 이동과 연결합니다.
- 상품 수가 디자인보다 많아질 경우 동일 카드 컴포넌트를 반복 사용합니다.

### 7.9 푸터

- 사이트맵, 운영 정보, 고객센터, 약관, 개인정보처리방침, 저작권 정보를 표시합니다.
- 뉴스레터 또는 이메일 입력 UI가 디자인에 존재하는 경우 입력과 제출 상태를 구현합니다.
- 실제 구독 API는 연결하지 않고 제출 완료 목업 상태만 제공합니다.
- 모바일에서는 컬럼을 세로로 쌓고 아코디언은 디자인에 명시된 경우에만 사용합니다.

---

## 8. 화면 목록과 목적

### 8.1 메인 페이지

- 목적: N SEOUL TOWER의 핵심 경험과 주요 콘텐츠를 한 페이지에서 소개
- 주요 행동:
  - 메뉴 선택
  - 언어 메뉴 열기
  - 이벤트 카드 선택
  - 추천 코스 상세 이동
  - N Pass 상세 이동
  - 굿즈 카드 탐색 및 상세 이동
  - 푸터 링크 이동
- 필요한 정보:
  - 브랜드 로고 및 메뉴
  - 날씨 또는 운영 정보
  - 이벤트 목록
  - 대표 추천 코스
  - N Pass 소개
  - 기프트숍 상품 목록
  - 커스텀 굿즈 목록
  - 사이트 기본 정보
- 이동 경로:
  - 외부 또는 로고를 통해 메인 진입
  - 각 섹션의 카드와 CTA를 통해 상세 페이지 이동

### 8.2 언어 선택 메뉴

- 목적: 사용 언어 선택
- 주요 행동: 언어 목록 열기, 언어 선택, 외부 영역 클릭 또는 ESC로 닫기
- 필요한 정보: 제공 언어 목록, 현재 선택 언어
- 이동 경로: 헤더 언어 버튼에서 진입 후 현재 페이지 유지 또는 언어별 URL 이동

### 8.3 상품·콘텐츠 카드 상세 이동

- 목적: 사용자가 관심 콘텐츠의 상세 정보를 확인하도록 연결
- 주요 행동: 카드 또는 CTA 클릭
- 필요한 정보: 제목, 이미지, 설명, 링크
- 이동 경로: 메인 페이지에서 각 상세 페이지로 이동

### 8.4 cta(toc)버튼으로 섹션간 이동
- 목적 : 사용자가 언제든 원하는 섹션으로 이동 할수 있도록 제공
- 주요 행동 : 
  - 페이지 우측 가운데의 line선택시 그에 맞는 섹션 이동
  - 사용자의 스크롤에 맞춰 line도 그에 맞춰 변함 
- 필요한 정보 : 
  - main, events, course, n pass, n gift shop, custom goods로 이동할수있는 라인(버튼)
- 이동 경로: 해당하는 섹션 버튼을 누르면 그 섹션으로 이동

---

## 9. 페이지 구조

```text
body
└── site_wrapper
    ├── site_header
    │   ├── brand_logo
    │   ├── primary_navigation
    │   ├── weather_summary
    │   └── language_selector
    ├── main_content
    │   ├── hero_section
    │   ├── events_section
    │   ├── recommended_course_section
    │   ├── n_pass_section
    │   ├── gift_shop_section
    │   ├── custom_tower_visual_section
    │   └── custom_goods_section
    └── site_footer
```

---

## 10. 사용자 흐름

### 10.1 방문 콘텐츠 탐색

1. 사용자가 메인 페이지에 진입합니다.
2. 히어로에서 N SEOUL TOWER 브랜드와 현재 정보를 확인합니다.
3. 아래로 스크롤해 이벤트와 추천 코스를 확인합니다.
4. 관심 있는 카드 또는 CTA를 클릭합니다.
5. 해당 상세 페이지로 이동합니다.

### 10.2 N Pass 탐색

1. 사용자가 메인 페이지에서 N Pass 영역까지 스크롤합니다.
2. N Pass의 핵심 설명과 티켓 비주얼을 확인합니다.
3. CTA 버튼을 클릭합니다.
4. N Pass 상세 또는 구매 안내 페이지로 이동합니다.

### 10.3 굿즈 탐색

1. 사용자가 N Gift Shop 또는 Custom Goods 영역에 진입합니다.
2. 상품 카드를 가로 또는 그리드 형태로 탐색합니다.
3. 관심 상품을 선택합니다.
4. 상품 상세 또는 굿즈 안내 페이지로 이동합니다.

### 10.4 언어 변경

1. 사용자가 헤더의 언어 선택 버튼을 클릭합니다.
2. 언어 목록이 열리고 현재 선택 언어가 표시됩니다.
3. 사용자가 다른 언어를 선택합니다.
4. 언어별 페이지로 이동하거나 목업 상태에서 선택값만 변경됩니다.

## 10.5 섹션간 이동
1. 사용자가 원하는 섹션으로 이동하기 위해 화면 중앙 오른쪽의 라인(버튼)을 클릭합니다.
2. 해당 섹션으로 이동합니다.
3. 그 라인은 해당 섹션을 보여줄때마다 라인이 따라 움직인다.

---

## 11. 섹션별 레이아웃 요구사항

### 11.1 공통

- 데스크톱 최대 콘텐츠 폭은 약 `1920px`을 기준으로 하되 디자인 원본에 맞춰 조정합니다.
- 페이지 양쪽 여백은 1920px에서 80%만큼 보이도록 해줍니다.
- 전체 섹션이 동일한 폭으로 정렬될 필요는 없으며, 시안처럼 일부 오브젝트는 콘텐츠 폭 밖으로 확장할 수 있습니다.
- 장식 오브젝트로 인해 페이지 전체 가로 스크롤이 생기지 않도록 상위 래퍼에 적절한 `overflow: clip` 또는 `hidden`을 적용합니다.
- 섹션 간 간격은 데스크톱에서 넉넉하게(최소 180px) 유지합니다.
- 이미지 비율은 `object-fit: cover` 또는 `contain`을 목적에 맞게 구분합니다.

### 11.2 히어로

- 데스크톱 1920px에서 대형 타이포그래피가 화면 너비의 주요 영역을 차지합니다.
- 타워 이미지는 제목의 `SEOUL`과 `TOWER` 사이 또는 디자인 시안 위치에 배치합니다.
- 타워는 원본 비율을 유지하며 임의로 늘리거나 압축하지 않습니다.
- 보조 정보는 히어로의 좌측 하단과 우측 상단 등 시안의 비대칭 위치를 유지합니다.
- 텍스트가 화면 밖으로 잘리는 디자인 요소인지, 실제 정보 손실인지 구분해 구현합니다.

### 11.3 이벤트

- 섹션 타이틀은 좌측 정렬을 기본으로 합니다.
- 이벤트 카드와 곤돌라 오브젝트는 선의 곡선을 따라 배치된 것처럼 보이도록 구성합니다.
- 곡선은 SVG 경로를 우선 사용합니다.
- 카드 간 높이와 위치 차이를 유지해 평면적인 그리드처럼 보이지 않게 합니다.
- 카드의 실제 클릭 영역은 카드 전체로 설정하되 내부 버튼의 포커스 구조를 중복시키지 않습니다.

### 11.4 추천 코스

- 섹션 제목은 중앙 정렬을 기본으로 합니다.
- 대표 카드의 배경 장면은 넓은 가로 비율을 유지합니다.
- 카드 내부 텍스트 패널은 이미지와 대비되도록 별도 표면을 사용합니다.
- 캐릭터와 타워 오브젝트는 제공 에셋의 원본 크기 비율을 유지합니다.


### 11.5 N Pass

- 티켓은 데스크톱에서 약 2:1 이상의 가로 비율로 표현합니다.
- 좌측은 텍스트와 CTA, 우측은 짙은 그린 표면으로 구분합니다.
- 티켓 절취부는 양쪽 중앙에 동일한 크기로 배치합니다.
- 티켓 위쪽 또는 주변의 보조 티켓 이미지는 절대 위치를 사용할 수 있으나, 태블릿 이하에서 겹침이 없도록 재배치합니다.

### 11.6 N Gift Shop

- 섹션 제목과 설명은 좌측 정렬합니다.
- 상품 카드는 약간씩 회전하거나 높이 차이를 둘 수 있으나 정확한 값은 시안에 맞춥니다.
- 카드 이미지가 잘리지 않도록 상품 유형에 따라 `contain`을 사용합니다.
- 모바일에서 첫 카드의 일부와 다음 카드가 함께 보여 가로 스크롤 가능성을 암시하도록 구성할 수 있습니다.

### 11.7 커스텀 타워 비주얼

- 긴 세로 공간을 사용하며 각 파츠 사이에 충분한 여백을 둡니다.
- 오브젝트 위치는 완벽한 일직선보다 시안처럼 약간의 좌우 이동을 허용합니다.
- 최종 완성 타워는 하단 콘텐츠와 자연스럽게 연결합니다.
- 단순 이미지 한 장으로 대체하지 않고, 제공된 개별 파츠가 있을 경우 각 요소를 분리 배치합니다.

### 11.8 Custom Goods

- 소개 텍스트와 대표 이미지의 시각적 균형을 유지합니다.
- 상품 카드는 데스크톱에서 4개 내외가 한 줄에 보이도록 구성합니다.
- 카드 안의 텍스트 길이는 2줄 이내를 기본으로 하고 초과 시 말줄임 처리 여부는 디자인 기준을 따릅니다.
- 우측 화살표 버튼은 최소 44px 클릭 영역을 확보합니다.

### 11.9 푸터

- 배경은 진한 회색 또는 디자인 원본의 다크 컬러를 사용합니다.
- 푸터 상단의 타워 실루엣을 제공 에셋으로 배치합니다.
- 좌측에는 사이트맵과 사업자 정보, 우측에는 뉴스레터 또는 관련 입력 영역을 배치합니다.
- 저작권 정보는 푸터 하단 구분선 아래에 배치합니다.

---

## 12. 화면 상태

### 12.1 기본 상태

- 모든 섹션의 대표 콘텐츠가 표시됩니다.
- 카드와 버튼은 클릭 가능한 상태입니다.
- 언어 선택 메뉴는 닫혀 있습니다.
- 가로 스크롤형 콘텐츠는 첫 번째 항목부터 표시됩니다.

### 12.2 로딩 상태

- 정적 HTML 중심 구현에서는 전체 페이지 로딩 스피너를 사용하지 않습니다.
- 이미지 로딩 전에는 이미지 비율을 유지하는 배경 또는 스켈레톤을 사용해 레이아웃 이동을 방지합니다.
- 지연 로딩 대상은 첫 화면 아래 이미지로 제한합니다.
- 히어로 핵심 이미지는 지연 로딩하지 않습니다.

### 12.3 빈 상태

- 이벤트 데이터가 없을 경우:
  - `현재 진행 중인 이벤트가 없습니다.` 문구를 표시합니다.
  - 이벤트 섹션 장식만 남기지 않고 카드 영역 높이를 축소합니다.
- 추천 코스 데이터가 없을 경우:
  - 섹션 자체를 숨기거나 기본 코스 데이터를 노출합니다.
  - 처리 방식은 운영 정책 확인 후 확정합니다.
- 굿즈 데이터가 없을 경우:
  - `상품 준비 중입니다.` 문구와 기프트숍 이동 링크를 표시합니다.

### 12.4 오류 상태

- 이미지 로드 실패 시 대체 이미지 또는 배경색을 표시합니다.
- 링크 URL이 없는 카드에는 클릭 상태를 제공하지 않습니다.
- 목업 폼 제출 실패 시 입력 하단에 오류 메시지를 표시합니다.
- 오류 메시지는 원인과 다음 행동을 함께 안내합니다.

### 12.5 비활성 상태

- 링크가 없는 CTA는 비활성화하며 `disabled` 또는 `aria-disabled="true"`를 사용합니다.
- 슬라이드의 첫 번째 또는 마지막 위치에서 더 이동할 수 없는 화살표는 비활성화합니다.
- 필수 입력이 비어 있으면 제출 버튼을 비활성화합니다.

### 12.6 Hover·Focus 상태

- 카드 hover 시 이미지 확대, 그림자 변화, 화살표 이동 중 1~2개 효과만 사용합니다.
- 버튼 hover 시 배경색과 텍스트 대비가 유지되어야 합니다.
- 키보드 포커스는 최소 2px 이상의 명확한 외곽선으로 표시합니다.
- hover만으로 전달되는 정보가 없어야 합니다.

---

## 13. 데이터와 저장

### 13.1 데이터 출처

초기 구현은 정적 JavaScript 객체 또는 JSON 목업 데이터를 사용합니다.

```js
const mainPageData = {
  events: [],
  recommendedCourse: {},
  nPass: {},
  giftShopItems: [],
  customGoodsItems: []
};
```

### 13.2 이벤트 데이터 예시

```js
{
  id: "event_01",
  title: "Event title",
  category: "Event",
  period: "2026.00.00 - 2026.00.00",
  image: "./assets/images/events/event_01.webp",
  href: "./events/detail.html?id=event_01"
}
```

### 13.3 코스 데이터 예시

```js
{
  id: "speed_course",
  label: "Speed Course",
  title: "See the Highlights in Less Time",
  description: "대표 코스 설명",
  image: "./assets/images/courses/speed_course.webp",
  href: "./courses/speed.html"
}
```

### 13.4 굿즈 데이터 예시

```js
{
  id: "goods_01",
  title: "상품명",
  description: "짧은 설명",
  image: "./assets/images/goods/goods_01.webp",
  href: "./gift-shop/detail.html?id=goods_01"
}
```

### 13.5 브라우저 저장

- 언어 선택값 저장이 필요한 경우에만 `localStorage`를 사용합니다.
- key 예시: `n_seoul_tower_language`
- 단순 UI 구현에서는 저장하지 않고 현재 세션에서만 상태를 유지해도 됩니다.
- 개인정보, 이메일, 결제정보는 브라우저에 저장하지 않습니다.

---

## 14. 개발 조건

- HTML, CSS, JavaScript로 구현합니다.
- React, Vue, TypeScript, Tailwind를 추가하지 않습니다.
- 기존 폴더 구조와 공통 스타일을 우선 유지합니다.
- 공통 헤더, 푸터, 버튼, 카드가 이미 존재하면 재사용합니다.
- 요청 없이 npm 패키지 또는 외부 라이브러리를 추가하지 않습니다.
- 이미지 경로는 하드코딩을 최소화하고 데이터 객체 또는 공통 경로 상수로 관리합니다.
- 모든 파일은 UTF-8로 저장합니다.
- 인라인 스타일과 인라인 이벤트 핸들러 사용을 피합니다.
- HTML은 의미 구조에 맞게 `header`, `nav`, `main`, `section`, `article`, `footer`를 사용합니다.

### 14.1 권장 파일 구조

```text
n_seoul_tower/
├── index.html
├── assets/
│   ├── images/
│   │   ├── hero/
│   │   ├── events/
│   │   ├── courses/
│   │   ├── pass/
│   │   ├── gift_shop/
│   │   ├── custom_goods/
│   │   └── common/
│   ├── icons/
│   └── fonts/
├── css/
│   ├── reset.css
│   ├── common.css
│   └── main.css
└── js/
    ├── common.js
    └── main.js
```

---

## 15. 명명 규칙

- CSS class와 HTML id는 `snake_case`를 사용합니다.
- BEM 형태를 혼합하지 않고 프로젝트 기존 규칙을 우선합니다.
- 상태 class는 `is_active`, `is_open`, `is_selected`, `is_disabled` 형식으로 작성합니다.
- 오류 class는 `has_error` 형식으로 작성합니다.
- JavaScript 변수와 함수는 `camelCase`를 사용합니다.
- 불리언은 `is`, `has`, `can`, `should`로 시작합니다.
- 이벤트 함수는 `handleXxx` 형식으로 작성합니다.
- 데이터 렌더링 함수는 `renderXxx` 형식으로 작성합니다.
- DOM 선택자는 역할이 드러나는 `data-*` 속성을 우선 사용합니다.

예시:

```html
<button
  class="language_button"
  type="button"
  data-language-button
  aria-expanded="false"
>
  EN
</button>
```

```js
const languageButton = document.querySelector("[data-language-button]");

function handleLanguageToggle() {
  // ...
}
```

---

## 16. 반응형 기준

### 16.1 기준 해상도

- 모바일: 360px
- 태블릿: 834px
- 데스크톱: 1920px

### 16.2 브레이크포인트

```css
/* 1. 데스크톱 (1920px 기준 기본 스타일) */
/* `@media` 밖에는 1920px 화면을 기준으로 한 PC 스타일을 작성합니다. */

@media (max-width: 1919px) {
  /* 해상도가 1920px보다 작아질 때 적용할 반응형 처리 */
}

@media (max-width: 834px) {
  /* 2. 태블릿 (834px 이하) 스타일 */
}

@media (max-width: 360px) {
  /* 3. 모바일 (360px 이하) 스타일 */
}
```

- 구현 방식은 데스크톱 퍼스트 기존 프로젝트 방식에 맞추되, 시각 검수는 1920px을 우선합니다.
- 1280px 이상에서는 데스크톱 구조를 유지하고 1920px에서 디자인 기준 폭과 여백을 맞춥니다.

### 16.3 데스크톱 1920px

- 디자인 시안의 비정형 배치와 넓은 여백을 그대로 유지합니다.
- 메인 콘텐츠 최대 폭은 약 1680px 기준입니다.
- 히어로 타이포그래피는 가장 큰 크기로 표시합니다.
- 이벤트 카드, 곤돌라, 점선 경로는 한 화면 안에서 넓게 분산 배치합니다.
- N Pass는 가로형 티켓 레이아웃을 유지합니다.
- 상품 목록은 한 줄에 여러 카드가 보이도록 합니다.
- 커스텀 타워 파츠는 긴 세로 스크롤을 활용합니다.

### 16.4 태블릿 834px

- 헤더 메뉴는 공간이 부족하면 메뉴 버튼으로 전환합니다.
- 히어로 타이포그래피와 타워의 겹침은 유지하되 크기를 축소합니다.
- 이벤트의 곡선형 구성은 단순화하고 카드 2열 또는 가로 스크롤로 전환합니다.
- 추천 코스 카드는 이미지와 텍스트를 겹치거나 상하 배치합니다.
- N Pass는 비율을 유지하되 텍스트 영역을 확대하고 장식 이미지를 축소합니다.
- Gift Shop과 Custom Goods는 2~3개 카드가 보이는 가로 스크롤을 허용합니다.
- 커스텀 타워 파츠의 좌우 이동 폭을 줄입니다.

### 16.5 모바일 360px

- 모든 주요 콘텐츠는 1열 구조를 기본으로 합니다.
- 헤더는 로고, 메뉴 버튼, 언어 버튼 중심으로 단순화합니다.
- 히어로 제목은 여러 줄로 나누며 타워가 텍스트를 가리지 않게 재배치합니다.
- 장식용 메타 정보는 중요도에 따라 축소하거나 다음 줄로 이동합니다.
- 이벤트의 점선 경로는 세로형 또는 단순 곡선으로 변경합니다.
- 이벤트 카드와 굿즈 카드는 가로 스크롤형으로 구현할 수 있습니다.
- N Pass 티켓은 세로형 카드로 전환하거나 내부를 상하로 분리합니다.
- 티켓 절취부가 콘텐츠를 침범하지 않도록 크기를 줄입니다.
- 커스텀 타워 파츠는 화면 중앙에 배치하고 파츠 간 간격을 축소합니다.
- 푸터 컬럼은 세로로 쌓습니다.
- 버튼과 링크의 최소 터치 영역은 44px × 44px 이상을 확보합니다.
- 폰트는 최소 14px 이상을 기본으로 하고 설명 텍스트의 가독성을 유지합니다.

### 16.6 공통 반응형 조건

- 모든 해상도에서 `100vw` 사용으로 인한 스크롤바 포함 오버플로를 피합니다.
- 절대 위치 오브젝트는 컨테이너 기준으로 배치합니다.
- 텍스트가 이미지 위에 올라가는 경우 최소 명도 대비를 확보합니다.
- 줄바꿈 위치는 `max-width`, `clamp()`, `<br>` 사용 여부를 디자인과 비교해 결정합니다.
- 콘텐츠 순서는 화면 크기가 바뀌어도 의미적으로 동일해야 합니다.

---

## 17. 접근성

- 클릭 동작은 `button`, 페이지 이동은 `a` 요소를 사용합니다.
- 현재 페이지를 가리키는 내비게이션에는 `aria-current="page"`를 사용합니다.
- 언어 메뉴 버튼에는 `aria-expanded`, `aria-controls`를 적용합니다.
- 메뉴는 ESC로 닫을 수 있어야 하며, 외부 클릭 시에도 닫힙니다.
- 모든 의미 있는 이미지에는 구체적인 `alt`를 제공합니다.
- 순수 장식 이미지는 `alt=""`와 필요 시 `aria-hidden="true"`를 사용합니다.
- 배경 이미지에 중요한 정보가 포함되지 않게 합니다.
- 키보드 Tab 순서가 시각적 순서와 일치해야 합니다.
- `focus-visible` 상태를 명확하게 표시합니다.
- 텍스트와 배경의 명도 대비는 WCAG AA 수준을 목표로 합니다.
- 색상만으로 활성, 비활성, 오류 상태를 구분하지 않습니다.
- 가로 스크롤 목록에는 키보드로 이동 가능한 버튼 또는 자연 스크롤 접근을 제공합니다.
- 자동 재생 모션은 사용하지 않거나 일시정지 수단을 제공합니다.
- `prefers-reduced-motion: reduce`에서 이동, 회전, 확대 모션을 제거합니다.
- 폼 입력에는 `label` 또는 접근 가능한 이름을 제공합니다.
- 오류 메시지는 입력과 `aria-describedby`로 연결합니다.

---

## 18. 인터랙션 및 모션

### 18.1 기본 원칙

- 스크롤 모션은 콘텐츠의 등장과 브랜드 경험을 강화하는 수준으로 제한합니다.
- 스크롤을 막거나 지나치게 느리게 만드는 연출은 사용하지 않습니다.
- 같은 페이지에서 여러 종류의 과도한 모션을 혼합하지 않습니다.
- 모션 지속 시간은 대체로 300~800ms 범위에서 사용합니다.

### 18.2 허용 모션

- 히어로 타이포그래피와 타워의 가벼운 페이드 또는 상승
- 이벤트 카드의 순차 등장
- 곤돌라 오브젝트의 미세한 부유 효과
- 추천 코스 카드의 이미지 패럴랙스 또는 페이드
- N Pass 티켓의 좌우 등장
- Gift Shop 카드의 미세한 회전 복귀
- 커스텀 타워 파츠의 순차 조립형 등장
- Custom Goods 카드의 순차 등장

### 18.3 라이브러리 사용

- CSS와 기본 JavaScript로 가능한 동작은 별도 라이브러리 없이 구현합니다.
- GSAP은 커스텀 타워 조립처럼 복잡한 스크롤 연동 모션이 필요한 경우에만 사용합니다.
- ScrollTrigger는 스크롤 위치와 직접 연결된 모션에만 사용합니다.
- Lenis는 부드러운 스크롤이 명시된 제품 요구사항일 때만 사용합니다.
- Swiper는 실제 카드 슬라이더 또는 갤러리에만 사용합니다.
- 라이브러리 사용 전 기존 프로젝트 설치 여부를 확인합니다.

### 18.4 Reduced Motion

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 19. 성능 기준

- 히어로의 핵심 이미지는 WebP 또는 AVIF를 우선 사용하되 원본 품질을 유지합니다.
- 첫 화면 이미지에는 적절한 `fetchpriority="high"`를 적용할 수 있습니다.
- 첫 화면 아래 이미지는 `loading="lazy"`를 사용합니다.
- 모든 이미지에 `width`, `height` 또는 `aspect-ratio`를 지정해 CLS를 방지합니다.
- 과도한 고해상도 이미지를 그대로 사용하지 않고 화면 크기에 맞는 소스를 제공합니다.
- `srcset`과 `sizes`를 사용해 360px, 834px, 1920px에 적절한 이미지를 전달합니다.
- SVG 장식은 DOM이 지나치게 복잡해지지 않도록 최적화합니다.
- 스크롤 이벤트는 `requestAnimationFrame` 또는 Intersection Observer를 우선 사용합니다.
- 사용하지 않는 라이브러리와 폰트를 로드하지 않습니다.
- 콘솔 오류와 404 에셋 요청이 없어야 합니다.

---

## 20. SEO 및 메타 정보

- 페이지별 고유 `title`과 `meta description`을 설정합니다.
- Open Graph 이미지, 제목, 설명을 설정합니다.
- 대표 이미지의 비율과 파일 용량을 확인합니다.
- `h1`은 메인 페이지에서 하나만 사용합니다.
- 섹션 제목은 문서 구조에 맞게 `h2`, 카드 제목은 `h3`를 사용합니다.
- 의미 없는 제목 태그를 장식용으로 사용하지 않습니다.
- 주요 링크 문구는 `View More`만 반복하지 않고 목적을 식별할 수 있도록 접근 가능한 이름을 제공합니다.
- 사이트 기본 언어에 맞는 `lang` 속성을 설정합니다.

---

## 21. 콘텐츠 작성 기준

- 디자인 시안에 있는 영문 표기를 우선 사용합니다.
- 한국어, 영어, 일본어, 중국어 콘텐츠가 필요한 경우 동일한 데이터 구조로 관리합니다.
- 제목은 짧고 명확하게 작성합니다.
- 설명은 카드당 2~3줄을 넘지 않도록 합니다.
- CTA 문구는 동일 행동에 동일 용어를 사용합니다.
- 예:
  - 상세 보기: `View Details`
  - 코스 보기: `Explore Course`
  - N Pass 보기: `Discover N Pass`
  - 상품 보기: `View Product`
- 실제 운영 문구가 확정되지 않은 부분은 임의로 최종 문구처럼 작성하지 않고 `TODO`로 표시합니다.

---

## 22. 검증 방법

### 22.1 디자인 검증

- Figma 또는 제공 시안과 브라우저 화면을 나란히 비교합니다.
- 1920px에서 섹션 높이, 오브젝트 위치, 카드 크기, 여백을 우선 검수합니다.
- 히어로 타이포그래피와 타워 이미지의 겹침 위치를 확인합니다.
- 이벤트 곡선, 곤돌라, 카드 위치를 확인합니다.
- N Pass 티켓의 절취부와 좌우 비율을 확인합니다.
- 커스텀 타워 파츠의 순서와 간격을 확인합니다.
- 푸터의 타워 실루엣과 컬럼 배치를 확인합니다.

### 22.2 반응형 검증

다음 화면에서 직접 확인합니다.

- 360 × 800
- 834 × 1112
- 1280 × 800
- 1920 × 1080

검증 항목:

- 페이지 전체 가로 스크롤 여부
- 텍스트 잘림과 의도하지 않은 줄바꿈
- 카드 겹침
- 절대 위치 오브젝트 이탈
- 이미지 비율 왜곡
- 버튼 터치 영역
- 푸터 컬럼 정렬
- 모바일 메뉴 열림·닫힘

### 22.3 기능 검증

- 로고, 메뉴, 카드, CTA, 푸터 링크를 모두 클릭합니다.
- 언어 메뉴를 마우스와 키보드로 조작합니다.
- ESC와 외부 클릭으로 메뉴가 닫히는지 확인합니다.
- 가로 스크롤 목록을 터치, 트랙패드, 키보드로 확인합니다.
- 빈 데이터와 링크 없는 데이터 상태를 확인합니다.
- 이미지 로드 실패 상태를 확인합니다.

### 22.4 접근성 검증

- 키보드 Tab 순서를 확인합니다.
- 모든 인터랙션 요소의 `focus-visible`을 확인합니다.
- 이미지 alt를 확인합니다.
- Lighthouse 접근성 검사를 진행합니다.
- `prefers-reduced-motion` 환경에서 모션이 축소되는지 확인합니다.
- 200% 확대에서 콘텐츠가 손실되지 않는지 확인합니다.

### 22.5 개발 검증

- 콘솔 오류와 경고를 확인합니다.
- 임시 `console.log`, 주석 처리된 테스트 코드, 사용하지 않는 파일을 제거합니다.
- 깨진 링크와 404 에셋을 확인합니다.
- HTML Validator 또는 브라우저 검사로 구조 오류를 확인합니다.
- CSS 중복 선언과 사용하지 않는 선택자를 점검합니다.

---

## 23. 완료 조건

- 제공된 메인 페이지 디자인의 전체 섹션이 구현되어 있습니다.
- 히어로, 이벤트, 추천 코스, N Pass, N Gift Shop, 커스텀 타워 비주얼, Custom Goods, 푸터가 누락 없이 존재합니다.
- 1920px에서 디자인 시안의 구도, 여백, 이미지 비율, 타이포그래피 계층이 일치합니다.
- 834px과 360px에서 콘텐츠가 자연스럽게 재배치됩니다.
- 페이지 전체 가로 스크롤이 없습니다.
- 모든 카드, 링크, 버튼, 메뉴가 정상 동작합니다.
- 이미지와 폰트 등 실제 에셋이 누락되지 않습니다.
- 키보드 탐색과 `focus-visible`이 동작합니다.
- `prefers-reduced-motion`이 반영됩니다.
- 콘솔 오류, 404 요청, 임시 로그가 없습니다.
- 변경 파일, 구현 내용, 검증 결과, 확인하지 못한 부분이 작업 보고서에 포함됩니다.

---

## 24. 구현 전 확인 필요 항목

아래 항목은 디자인 이미지에서 정확한 값을 확정하기 어려우므로 구현 전에 Figma 또는 기획자 확인이 필요합니다.

- 헤더의 전체 메뉴명과 실제 이동 URL
- 날씨 정보의 실제 API 사용 여부
- 히어로 보조 문구와 운영 시간
- 이벤트 카드 개수, 제목, 기간, 링크
- 추천 코스의 고정 노출 여부 또는 데이터 변경 방식
- N Pass CTA의 실제 목적지
- 기프트숍과 커스텀 굿즈의 실제 상품명, 가격, 링크
- 상품 카드 슬라이더 여부
- 커스텀 타워 파츠의 실제 스크롤 모션 강도
- 푸터 뉴스레터 또는 이메일 폼의 실제 기능
- 헤더 고정 여부
- 다국어 URL 구조
- 사용 폰트와 정확한 font-weight
- 색상, 라운드, 그림자, 간격의 최종 디자인 토큰

---

## 25. 작업 보고 형식

구현 완료 후 아래 형식으로 보고합니다.

```md
## 변경 파일

- index.html
- css/main.css
- js/main.js
- assets/...

## 구현 내용

- 메인 히어로 구현
- 이벤트 카드 및 곡선 장식 구현
- 추천 코스 구현
- N Pass 티켓 구현
- Gift Shop 및 Custom Goods 구현
- 반응형 구현
- 접근성 보완

## 검증 결과

- 360px: 통과 / 이슈
- 834px: 통과 / 이슈
- 1920px: 통과 / 이슈
- 키보드 접근성: 통과 / 이슈
- 콘솔 오류: 없음 / 있음

## 확인하지 못한 부분

- 실제 API 연결
- 운영 링크
- 최종 다국어 문구
```
